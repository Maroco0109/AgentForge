apiVersion: v1
kind: Service
metadata:
  name: redis
  namespace: agentforge
  labels:
    app: redis
    app.kubernetes.io/part-of: agentforge
spec:
  clusterIP: None
  selector:
    app: redis
  ports:
    - port: 6379
      targetPort: 6379
      name: redis
