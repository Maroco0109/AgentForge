apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: agentforge
  labels:
    app: backend
    app.kubernetes.io/part-of: agentforge
spec:
  type: ClusterIP
  selector:
    app: backend
  ports:
    - port: 8000
      targetPort: 8000
      name: http
