apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: agentforge
  labels:
    app: frontend
    app.kubernetes.io/part-of: agentforge
spec:
  type: ClusterIP
  selector:
    app: frontend
  ports:
    - port: 3000
      targetPort: 3000
      name: http
