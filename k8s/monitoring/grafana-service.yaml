apiVersion: v1
kind: Service
metadata:
  name: grafana
  namespace: agentforge
  labels:
    app: grafana
    app.kubernetes.io/part-of: agentforge
spec:
  type: ClusterIP
  selector:
    app: grafana
  ports:
    - port: 3000
      targetPort: 3000
      name: http
