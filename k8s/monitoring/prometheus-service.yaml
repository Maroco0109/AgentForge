apiVersion: v1
kind: Service
metadata:
  name: prometheus
  namespace: agentforge
  labels:
    app: prometheus
    app.kubernetes.io/part-of: agentforge
spec:
  type: ClusterIP
  selector:
    app: prometheus
  ports:
    - port: 9090
      targetPort: 9090
      name: http
