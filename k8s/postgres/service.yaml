apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: agentforge
  labels:
    app: postgres
    app.kubernetes.io/part-of: agentforge
spec:
  clusterIP: None
  selector:
    app: postgres
  ports:
    - port: 5432
      targetPort: 5432
      name: postgresql
