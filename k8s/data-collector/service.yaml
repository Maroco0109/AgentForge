apiVersion: v1
kind: Service
metadata:
  name: data-collector
  namespace: agentforge
  labels:
    app: data-collector
    app.kubernetes.io/part-of: agentforge
spec:
  type: ClusterIP
  selector:
    app: data-collector
  ports:
    - port: 8001
      targetPort: 8001
      name: http
